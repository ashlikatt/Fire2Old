let references = BigArray<Any>()
let referenceID: Int = 0

struct Reference<T> {
    id: Int,

    fn create<T>(any: T): Reference<T> {
        references[referenceID] = any;
        referenceID += 1;
        return Reference<T> {
            id: referenceID
        }
    }
}

